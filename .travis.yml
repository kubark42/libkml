language: cpp
compiler:
  - gcc
install:
  - ./autogen.sh
  - mkdir build
  - cd build
  - ../configure
  - make
  - sudo make install
script:
  - make test
notifications:
  email:
    on_success: change
    on_failure: always
os:
  - linux
  - osx
